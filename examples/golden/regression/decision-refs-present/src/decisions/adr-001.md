# ADR-lite placeholder for the decision-refs-present regression fixture.

This file is a self-contained placeholder that lives next to the
fixture's completion card. It is referenced from
`context_alignment.decision_refs` in the regression fixture so the
admission layer can resolve the entry to a real on-disk path under
the examples tree. The body of the file is intentionally empty
because the regression check is the path resolution, not the
decision content.
