# Product intent placeholder for the intent-ref-present regression fixture.

This file is a self-contained placeholder that lives next to the
fixture's completion card. It is referenced from the top-level
`intent_ref` field on the completion card (slug id
`doc/intake-lite.md`) so the engine can resolve the entry to a real
on-disk path under the fixture directory. The body of the file is
intentionally sparse because the regression check is the
admission-layer advisory suppression, not the product intent
content.
