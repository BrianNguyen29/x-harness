# Test Fixtures

## Purpose
This directory contains reference completion cards, claims, and evidence files used by the test suite to validate `x-harness verify` command parsing and error handling behaviors.

## Layout
- Test fixtures model expected structures (YAML and JSON formats).
- Used by unit tests to ensure that valid configurations succeed, schema violations are caught, and appropriate validation errors are printed.
