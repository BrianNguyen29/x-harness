# Quickstart

```bash
npx x-harness init --minimal
npx x-harness verify
npx x-harness doctor
npx x-harness report
```

Start minimal, then add product layer, schemas, adapters, or GitHub Actions when needed.
