# Quickstart

```bash
npx claimgate init --minimal
npx claimgate handoff light --title "Check config rule"
npx claimgate verify
npx claimgate doctor
npx claimgate report
```

Start minimal, then add product layer, schemas, adapters, or GitHub Actions when needed.
