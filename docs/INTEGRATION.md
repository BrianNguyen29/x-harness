# Integration

x-harness is file-first and adapter-second. Adapters project the same contract into OpenCode, Claude Code, Cursor, Antigravity, and generic AGENTS.md-compatible tools.

All adapters must preserve: verifier read-only, PGV advisory-only, canonical tiers, fail-closed admission, and no accepted completion without verify success.
