# FAQ

## Why TypeScript CLI?

Because `npx x-harness` is easy to run in modern developer workflows and CI. The CLI is not the source of truth; files are.

## Is Python required?

No. Python is not canonical core tooling.

## Does x-harness require an LLM to verify?

No. Verification is deterministic-first.
